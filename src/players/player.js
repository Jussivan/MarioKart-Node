export function Player(playerName, character, speed, control, power) {
    return {
        playerName: playerName,
        character: character,
        speed: speed,
        control: control,
        power: power
    };
}